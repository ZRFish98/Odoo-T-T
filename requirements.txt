# --- mandatory ---
streamlit==1.33.0          # latest stable, works on Py 3.13
pandas>=2.2.2,<2.3         # 2.2+ compiled wheels exist for Py 3.13
numpy>=2.0.0,<2.2          # NumPy 2.x no longer needs distutils
openpyxl>=3.1.2
# --- optional ---
xlrd>=2.0.1                # only if you need old .xls files
pdfplumber>=0.10.0         # if you use PDF extraction
